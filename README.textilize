h2. git_local

git_local is intended to provide a browser view of your local git repos.  It's pretty much useless in it's current state though.

h3. gems
* sinatra
* syntaxi
* grit
* yaml

h3. Usage
* add the repo to repos.yml (currently only supports one)
* start the server
<pre>ruby git_local.rb</pre>